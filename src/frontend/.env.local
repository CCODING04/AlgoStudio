# AlgoStudio Web Console Environment Variables

# API Base URL (FastAPI server)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# RBAC Secret Key for backend authentication (server-side only)
# This must match the RBAC_SECRET_KEY used by the API server
RBAC_SECRET_KEY=test-secret-key-for-development
